<?xml version="1.0" encoding="utf-8"?>
<!--
  Digital Pigmee — network security policy.

  Android 9 (API 28) and above reject plaintext HTTP by default, which would make
  every call to a local dev API fail regardless of the URL. The base config below
  keeps that secure default in force, so a production build talking to an https://
  endpoint is unaffected.

  The domain exceptions exist only so a demo/UAT build can reach an API served over
  plain HTTP on a developer machine:
    - 10.0.2.2      the Android emulator's alias for the host loopback
    - localhost      an API reached through `adb reverse`
    - 192.168.1.24   this build machine's LAN address, for a physical device

  Before shipping to the Play Store: point the app at an https:// endpoint and
  delete this file along with the android:networkSecurityConfig attribute in
  AndroidManifest.xml.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">192.168.1.24</domain>
    </domain-config>
</network-security-config>
